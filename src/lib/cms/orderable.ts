/**
 * Pages whose top-level sections render through the ordered-blocks pilot —
 * reorder and enable/disable from the admin apply here. Other pages keep
 * their hand-composed order (entangled hero/animation structures).
 */
export const ORDERABLE_PAGES: ReadonlySet<string> = new Set(["webapps"]);
